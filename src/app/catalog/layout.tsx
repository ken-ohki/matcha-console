import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'SABO WHOLESALE CATALOG',
}

export default function CatalogLayout({ children }: { children: React.ReactNode }) {
  return children
}
